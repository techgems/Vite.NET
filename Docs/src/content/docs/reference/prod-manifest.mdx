---
title: 'manifest.prod.json'
description: The production manifest the Vite plugin emits during a build, and how it pairs with Vite's manifest.json.
---

import { Badge, Aside } from '@astrojs/starlight/components';

<Badge text="Emitted by vite-dotnet" variant="note" /> <Badge text="Production" variant="tip" />

The production integration manifest, emitted during `vite build`. It carries the metadata the back
end can't get from Vite's own manifest. Consumed by
[`prod-vite-scripts`](/reference/prod-vite-scripts).

## Location

```
wwwroot/{appFolder}/manifest.prod.json
```

Emitted from the plugin's `generateBundle` hook, alongside Vite's own `manifest.json`.

## Shape

```json
{
  "entrypoint": "src/main.tsx",
  "containerElementId": "root",
  "isReact": true
}
```

| Field | Type | Description |
| --- | --- | --- |
| `entrypoint` | `string` | The entry module passed to `ViteDotNetPlugin`. |
| `containerElementId` | `string` | The mount element id passed to `ViteDotNetPlugin`. |
| `isReact` | `boolean` | Whether a React plugin was detected in the resolved config. |

There is no `port` — production serves the built assets statically.

<Aside type="note">
This maps to the `ProdManifestModel` record on the back end:
`record ProdManifestModel(string Entrypoint, string ContainerElementId, bool IsReact)`.
</Aside>

## Pairing with Vite's `manifest.json`

`manifest.prod.json` does not contain the hashed asset paths. Those live in Vite's standard build
manifest, `wwwroot/{appFolder}/manifest.json`, which maps your entrypoint to its output `file` and
associated `css`. The production tag helper reads **both**:

- **`manifest.json`** → the hashed JS and CSS paths to emit as `<script>` / `<link>` tags.
- **`manifest.prod.json`** → the `containerElementId` for the mount `<div>`.

<Aside type="tip">
Because the container id and React flag are carried by this file, the back end needs no
hand-written configuration beyond the app's folder name.
</Aside>

## Related

- [manifest.dev.json](/reference/dev-manifest) — the development counterpart.
- [Production Builds](/guides/production-builds) — the full build output.
